# Pomodoro-Timer
A simple pomodoro timer, using hybrid development method (HTML5, CSS3, and JavaScript) and also for the framework we use Vue.js (loaded via CDN), the app combines web technologies with native device capabilities through a hybrid approach, utilizing service workers for offline caching and localStorage for store and validate all data.
